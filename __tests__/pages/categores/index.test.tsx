// Critical paths (core user flows that must work).
// Complex functions (anything with intricate logic).
// User interactions (how users interact with your UI).

// 1) describe
// 2) it
// callback
// render(component)

// MOCK
// use mockcing for retrieving the data
// create testing where if hovered shows it correctly

// if fetching then loading sign
// if error then expect APIErrorBoundary

// SNAPSHOT
//

// have to test whether new set of books when pages load
