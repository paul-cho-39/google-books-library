// const IS_PRODUCTION = process.env.NODE_ENV !== 'development';

// export const deferMock = async () => {
//    // if it is in production then traffic should not be blocked
//    if (IS_PRODUCTION) {
//       return;
//    }
//    const { worker } = await import('./worker');
//    return worker.start();
// };
